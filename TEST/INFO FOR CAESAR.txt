Variables:

constant alphabet

Input string
Input shift

store current letter
store index in array
store index in array + shift
store encodedLetter

Output Encoded


Input string
Input shift

from(0 -> length of string)					#stored is the current position being encoded in string
	currentLetter = string[position];
	index in array = find in alphabet current letter
		Check if index = -1, if true output 'character (position in string, character) not within current alphabet'
	index in array = index in array + shift			//incriments index in array by the shift, positive or negative, recycled from before shift
		While( absolute value of index > size of alphabet)
			Subtract size of alphabet if too big, add if too small
	encoded letter = alphabet[index in array]		//index in array is recycled from before shift
	push onto encoded string

output final encoded string